#include <bits/stdc++.h>

using namespace std;

int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0);

	int fir, sec, thi;

	cin >> thi >> sec;

	fir = sec + (sec - thi);
	cout << fir;

	return 0;
}