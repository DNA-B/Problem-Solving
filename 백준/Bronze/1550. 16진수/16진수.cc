#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    int hex_num;

    cin >> hex >> hex_num;
    cout << hex_num;

    return 0;
}