#include <bits/stdc++.h>

using namespace std;

int main() 
{
    ios::sync_with_stdio(0);
	cin.tie(0);
    
	string str;
	cin >> str;

	string fan = ":fan:";

	cout << fan << fan << fan<<"\n";
	cout << fan << ":" << str << ":" << fan << "\n";
	cout << fan << fan << fan;

}