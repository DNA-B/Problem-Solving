#include <bits/stdc++.h>
#define INF 0x3f3f3f3f

using namespace std;


int main(void)
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    int N, M;

    cin >> N >> M;
    cout << (N * M) - 1;

    return 0;
}