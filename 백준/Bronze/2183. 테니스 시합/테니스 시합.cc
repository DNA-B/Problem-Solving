#include <bits/stdc++.h>
#define INF 0x3f3f3f3f
#define IDX 1000
using namespace std;

int N;
string s;

int main(void)
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> N >> s;
    cout << s[s.length() - 1];

    return 0;
}