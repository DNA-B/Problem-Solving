#include <bits/stdc++.h>

using namespace std;

int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0);

	bool n;

	cin >> n;

	if (n)
	{
		cout << "Leading the Way to the Future";
	}
	else
	{
		cout << "YONSEI";
	}


	return 0;
}