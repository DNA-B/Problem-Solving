#include <bits/stdc++.h>

using namespace std;

int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0);

	long long ur, tr, uo, to;
	cin >> ur >> tr >> uo >> to;
	cout << (56 * ur) + (24 * tr) + (14 * uo) + (6 * to);
	return 0;
}